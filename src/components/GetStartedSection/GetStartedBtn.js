import React from 'react'

const GetStartedBtn = ({buttontitle}) => {
  return (
    <button type="button" className="btn btn-getStarted fw-bold">{buttontitle}</button>
  )
}

export default GetStartedBtn;